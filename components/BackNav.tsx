import React from 'react';
import { StyleSheet, View } from 'react-native';

import BackNavIcon from '../assets/images/arrow.svg';

const BackNav = () => {
    return (
        <View>
            <BackNavIcon width={20} height={20} />
        </View>
    );
}

const styles = StyleSheet.create({})

export default BackNav;
